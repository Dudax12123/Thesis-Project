# Handoff — 2026-09-16

For the next session. Read `CLAUDE.md` first, then this file. It supersedes
`dev-notes/handoff-2026-09-13.md` (whose §1–§2 still hold except where marked below). If the work
touches the indirect PMP, also read §1 of `dev-notes/pmp-mass-costate-plan-2026-09-13.md` and the
memory note `pmp-frame-bibliography`. The auto-memory (`MEMORY.md`) loads by itself.

---

## 0. Starting the session

| repository | path | state on 2026-09-16 |
|---|---|---|
| code | `C:\Users\eduar\Desktop\Tese\Code\Thesis-Project` | `main` at `508f88f` (= `1aefdd8` + the 2026-09-16 work of §1, committed and pushed to GitHub the same evening); the only later change is this file's own §0/§1/§4 update |
| thesis (Overleaf git bridge) | `C:\Users\eduar\Desktop\Tese\Thesis_Overleaf` | `main` at `d45cac9`, clean, fetched and level with the remote (re-checked at the end of 2026-09-16) |

Launch from the code repo root with the thesis repo added, so one session can read both:

```
cd C:\Users\eduar\Desktop\Tese\Code\Thesis-Project
claude --add-dir "C:\Users\eduar\Desktop\Tese\Thesis_Overleaf"
```

(Desktop app: start the session in the code repo, then send
`/add-dir C:\Users\eduar\Desktop\Tese\Thesis_Overleaf` before anything else — the thesis path is
outside the working directory and is invisible until added.) First message:
`read dev-notes/handoff-2026-09-16.md`. The auto-memory (`MEMORY.md`) already points here.

Standing rules: close every answer with **done / flagged / recommended next**; commit only when asked
(`Co-Authored-By: Claude <model> <noreply@anthropic.com>`); `git fetch` the thesis repo and read the
incoming diff before editing it; never handle the Overleaf token (mask with
`sed -E 's#//[^@]*@#//***@#'`); no LaTeX toolchain — verify with `\ref`/`\label`/`\cite` sweeps.
**New 2026-09-16: every figure, including ad-hoc comparisons, is drawn with the project's plot
library** (`Plots/new_plot_runner.py`, `Plots/results_figures/`, `Archive/compare.py` /
`run_archive.py compare`) — never inline matplotlib. **Thesis (report) edits remain ON HOLD since
2026-09-10 — confirm the hold is lifted before editing Overleaf.**

---

## 1. What this session did (2026-09-16) — committed as `508f88f`, pushed

1. **Run survey.** All 103 archives under `Tese/src/Output/` classified against the fix commits
   (MECO `df7a781`, PEG/Apollo `2c9ab0c`, tangent `67441bd`, PMP `1aefdd8`). Result (§2): 6 cases
   valid at 100×500, 2 valid only at 100×250, 12 with no usable run. Every PMP archive is void.
2. **Chapter 6 inventory.** 9 tables + 16 figures mapped to the cases they read. All 16 PNGs in
   `Thesis_Overleaf/Figures/` were built 2026-09-09 from the pre-MECO batch → all to regenerate.
3. **PMP plot suite** (`Simulation/indirect_pso_solver.run_indirect_full`): pitch history now built
   on the dense output grid for the whole flight (the RHS-cadence record stopped at separation and
   carried five speculative samples past the 7.5 s kick — the sawtooth); SECO
   (`TIME_TO_STOP_BURNING_SINGLE_BURN_FINAL`) and coast-start (`PSO_COAST_ARC2_START_TIME`) globals
   set like `pso_coast`. Placed AFTER the trailing `run_indirect_trajectory` call, which re-runs
   Stage 1 and resets those globals (trap, §7). Test added to `tests/test_pmp_frame.py`.
4. **Stage-1 pseudo-forces ON for the PMP** (user decision): `INDIRECT_PMP_STAGE1_PSEUDO_FORCES = True`
   in `simulation_parameters.py`; `ips._stage1_pseudo_forces()` (raises with the legacy
   `"rotating"` Stage 2); both `set_pseudo_forces_for_run` sites in the solver; reference cache
   SCHEMA **v4** (+ `INCLUDE_PSEUDO_FORCES` key); `main.py` rotation print; `main.py` PMP branch now
   fills 08b/17/18 from `ra.pseudo_force_channels_on_grid` (applied in Stage 1, evaluated in
   Stage 2) with a title note threaded through `run_new_plot_suite(pseudo_forces_note=)`;
   `run_record` comment; `_data.force_model_note` adds the inertial-Stage-2 clause; docs
   (`CLAUDE.md`, `Tese/worktree.md`, `set_pseudo_forces_for_run` docstring). New
   `tests/test_pmp_stage1_pseudo_forces.py` (5 tests). **138 pass.**
5. **Frame-equivalence measurement** (numbers in §2): the inertial Stage 2 is NOT the same physics as
   the laws' rotating frame + pseudo-forces, except at the equator due east. Cause: the transform
   credits the full ω·r·cos φ along-track; the pseudo-force terms credit the azimuth-projected part
   along the drifting latitude. Not a bug — a convention.
6. **Bibliography check** (memory `pmp-frame-bibliography`): no in-bib source puts pseudo-forces in
   the PMP costates. `pallone2016` does rotating-frame lower stages + inertial costate arc (= the
   current design, equatorial); Morgado 2022 neglects Coriolis/centrifugal and adds the credit as a
   velocity; Vinh 1981 §3.3 is the rotating-frame precedent (not in the bib).
7. `pypdf` was pip-installed into `pygmo-env` (dev-only, for reading papers).

Files: `CLAUDE.md`, `Tese/worktree.md`, `Tese/src/{main.py, Archive/run_record.py,
Input_File/simulation_parameters.py, Simulation/indirect_pso_solver.py, Simulation/rocket_ascent.py,
Simulation/segment_reference.py, Plots/new_plot_runner.py, Plots/new_metrics/{pseudo_forces,
cross_heading_counter_force, cross_heading_accel}_over_time.py, Plots/results_figures/_data.py,
tests/test_pmp_frame.py}`; new `tests/test_pmp_stage1_pseudo_forces.py`; untracked
`dev-notes/handoff-2026-09-13.md`.

---

## 2. Numbers to carry (seed 42, post-MECO-fix)

**Valid runs per case** ("valid" = after the MECO fix and the fix to its own law, baseline pf on):

| case | best valid | where |
|---|---|---|
| `gt_baseline` | 20 647.0 kg, 100×500 (20 347.9 at 100×250: +300 kg budget sensitivity) | `pf_ab_partial/results_matrix_pf_on_v2` |
| `peg_baseline` | 17 402.5 kg, 100×500 (pf-off twin 19 838.0 → search-limited) | `results_matrix_pf_on_v3` |
| `show_cpr` / `show_apollo` / `show_peg` / `show_exp_shooting` | 17 593.6 / 14 623.8 / 8 027.7 / 18 045.2 kg, 100×500 | `results_matrix_pf_on_v3` |
| `show_linear_tangent` / `show_bilinear_tangent` | 20 405.9 / 16 603.0 kg, **100×250 only**, family reaches ~21 320 | `audit_fix/lts_prod`, `bts_prod` |
| the other 12 (`gt_apogee`, `gt_direct`, `gt_vacuum`, `gt_norot`, `gt_sea_level_engine`, `peg_direct`, `peg_vacuum`, `peg_vacuum_norot`, `pmp_baseline`, `pmp_vacuum`, `show_seg_fixed_alt`, `show_seg_opt_alt`) | **none** | — |

If the coast bound is unified (§3.1), the 8 valid runs go stale too (flown at 1000 s).

**Indirect PMP — everything found before 2026-09-16 is stale** (Stage 1 changed). With the new
Stage 1 the polished `outer` decision vector (22 268 kg) ends at 544.5 km, 7 017 m/s, γ −3.97°: the
propellant of a given x is unchanged (burn durations are fixed by x), only where it ends. The
2026-09-13 extremal family (20 191 → 22 268 kg, coast 263 → 1 883 s; ~22 460–22 580 at the 2 000 s
bound; 21 880 at coast ≤ 1 000 s) is now only a guide to the shape of the optimum.

**Stage-1 hand-off, pseudo-forces on vs off, same kick:** +9.09 km, −44.2 m/s, +4.48° γ
(79.76 km / 3 314 m/s / 20.83° vs 70.67 km / 3 358 m/s / 16.35°).

**Stage-2 frame divergence** (same hand-off; rotating + pseudo-forces vs inertial, converted back):
100 s coast 0.6 km / 0.7 m/s / 0.23°; 600 s 25.8 km / 57.5 m/s / 1.01°; 1 883 s 125 km / 296 m/s;
254 s burn at common pitch 5.2 km / 2.4 m/s / 0.40°. Equator due east: 0 to tolerance. Due east at
28.5°: 6.1 km / 14 m/s at 1 883 s (latitude drift only). Azimuth-projected credit in the transform:
burn 8 m / 2.3 m/s; 1 883 s coast still 43 km / 104 m/s (latitude + heading drift). Credit at
hand-off 413.8 (full) vs 292.5 m/s (projected): **121.3 m/s ≈ 944 kg** of Stage-2 propellant at
insertion (m_f 26 168 kg, Isp 348 s).

**PMP gravity loss** 1 570 m/s = 1 151 powered + 419 during the 1 883 s coast (definition kept).

`pso_coast` archived α in unpowered arcs is an interpolation artefact: `peg_baseline` ramps
−3.6° → −11.7° across its coast and reaches 51.9° during the 146–154 s separation drift.

---

## 3. To do — decisions for the user

1. **Coast bound** — PMP `PSO_UB[3]` 2 000 s vs `PSO_COAST_UB[0]` 1 000 s. Gates every run in §4; a
   unified bound voids the 8 valid runs too.
2. **Tangent-law search gap** — more budget / bound α at ignition / multi-start / report.
3. **Re-fly budgets** per architecture.
4. **Mass costate** — optional.
5. **Report hold** — lift or keep; scope of §6.
6. **Ignition attitude jump** — ON HOLD, do not implement unasked (visible as the θ step at
   149–154 s in the PMP pitch plot).
7. **Stage-2 rotation-credit convention** (new). Options: (a) project the credit everywhere —
   transform, laws' targets, budget gain — reversing the deliberately kept unprojected convention;
   (b) pseudo-forces in the PMP Stage 2 with λ_s (formulation change; **no in-bib precedent**,
   departs from `pallone2016` and Morgado 2022); (c) keep Stage 2 inertial, cite `pallone2016`
   (stating its equatorial restriction) and Morgado 2022, and report the 121 m/s ≈ 944 kg
   asymmetry in §6.4. Current lean after the bibliography check: (c), or (a) if one convention
   across all architectures is wanted.
8. **Zero α in unpowered arcs, all architectures** (`alpha_full[thrust_full == 0] = 0` at output
   assembly in `run_pso_coast_full`, `run_indirect_full`, direct, segmented). Changes only the
   archived α channel and the α panels of F6.6/F6.14.
9. Gravity loss over the whole flight — **decided: keep**; text must say so (T6.6/F6.10/F6.11).

---

## 4. To do — runs

1. Rebuild `pmp_reference.npz` (the tree is committed, `508f88f`; the key is schema v4; back up the
   tracked file; pass `--set PMP_REFERENCE_PSO_PARTICLES=… --set PMP_REFERENCE_PSO_GENERATIONS=…`).
   Do this only after decisions 1 and 7 (§3) — both change the reference.
2. Paired production PMP swarms under the new Stage 1 (250×1000, new vs `pontani_eq38` penalty) +
   LM/continuation polish (`dev-notes/pmp_swarm_polish.py`). All previous x vectors are stale.
3. Re-fly `pmp_baseline`, `pmp_vacuum` through the harness.
4. Re-fly the 12 cases with no valid run (`--only`), starting with the five cheap GT ones
   (`gt_apogee` is 48 s) so F6.2–F6.5 unblock early; all 20 if §3.1 changes the bound.
5. Regenerate the 16 chapter figures from one consistent batch (`Plots/results_figures/make_all`).
6. Optional: second-seed repeats of `show_cpr` / `show_exp_shooting`.

---

## 5. To do — code

1. **NaN eccentricity** — `rocket_ascent.py:841` `sqrt(1 − h²/(μa))` rounds negative for a
   near-circle → `periapsis_km` NaN → `_data.Case.reached_orbit` False → F6.12/F6.13 hatch the PMP
   out. Clamp at 0.
2. `CLAUDE.md` stale lines: matrix runtime "~13-14 h" (measured ~25.7 h at 100×500); tests
   "31 tests in two files" (now 138 in 9 files).
3. Pinned-coast polish from the best converged extremal (after §4.2).
4. Legacy `apogee_check` pitch is still recorded from the ODE RHS (`run()` → `rocket_dynamics`):
   same speculative-sample sawtooth around every terminal event. Plot-only.
5. `run_archive.py replay` does not pass `pseudo_forces_note` for PMP archives (title lacks the
   "evaluated, not applied" caveat).
6. `exp_shooting` re-epoched per arc (flagged 09-11, never requested).
7. Low priority: `event_second_engine_ignition` still latched (legacy path only); `peg`
   non-reproducible under the planned-t_go path; arc-1 direct-insertion targeting documented, not
   changed; Pontani (2014) H definition in Eq. 38.
8. Memory notes updated this session: `pseudo-forces-all-or-nothing`, `pmp-stage2-inertial-frame`
   (2026-09-16 addenda), new `plots-via-project-library`, `pmp-frame-bibliography`.

---

## 6. To do — thesis (only once the hold is lifted)

Files in `Thesis_Overleaf`; line numbers as of `d45cac9` — re-grep after fetching.

- **Chapter 6, `Thesis_Results.tex`:** T6.1 (l.142–178) says 100×250 — runs are 100×500; add the
  coast bound. l.721–724 residual prose: now two causes to state — the unprojected budget credit
  AND the PMP's inertial-frame credit (§2). l.857 "the achievable optimum"; l.865 F6.12 caption
  "against the indirect reference" + hatching; F6.13 caption (dashed PMP line); l.889
  `tab:law_summary` "Shortfall" sign convention. T6.6/F6.10/F6.11 must state the gravity loss runs
  over the whole flight, coasts included. §6.4 must state the PMP force-model differences: Stage 2
  inertial with the unprojected credit (numbers in §2); drag-free Stage 2 (shared by all). Every
  number from the re-fly (§4).
- **Chapter 3, `Thesis_Optimization_Background.tex`:** l.641–663 ("flown without the rotating-frame
  pseudo-forces … in neither stage") is now wrong — Stage 1 carries them; Stage 2 inertial. Cite
  `pallone2016` Eqs. 10–11 (rotating-frame lower stages), 17–18 (planar transform), 34–38 (inertial
  costates = Eqs. costate_r/v/γ), noting its equatorial restriction; Morgado 2022 for the additive
  credit. Transversality conditions of the solver's own durations replacing Eq. 38 as coded.
- **Chapter 5, `Thesis_Methodology.tex`:** l.235–245 exemption paragraph ("omitted from the whole
  ascent") → Stage 1 on, Stage 2 inertial; `INDIRECT_PMP_STAGE1_PSEUDO_FORCES` in the settings table
  (l.328 area); LM + γ_p continuation polish; drag-free Stage 2; coast bound; single seed.
- **Chapter 4, `Thesis_Guidance.tex`:** classical PEG `C` term; Apollo local frame + Luminary P12
  thrust-magnitude rule; open-loop tangent laws; Table 4.1 t_go column.
- **Chapter 2/5:** lift is α-independent (constant C_L = 0.1) — state it if not already.
- Ignore `Legacy Chapters/` in the Overleaf repo.

---

## 7. Traps

- **`run_indirect_full` calls `run_indirect_trajectory` at its end, which re-runs Stage 1;
  `run_stage1`'s reset block empties `theta_history` and sets the SECO global back to `None`.**
  Anything written to those globals before that call is silently undone.
- `dev-notes/pmp_swarm_polish.py` / `pmp_local_refine.py` JSONs hold decision vectors polished under
  the pseudo-force-free Stage 1 — do not restart a polish from them expecting orbit.
- `INDIRECT_PMP_STAGE1_PSEUDO_FORCES=True` + `INDIRECT_PMP_STAGE2_FRAME="rotating"` raises (two force
  models in one ascent); set the flag False to reproduce pre-09-16 rows.
- `pseudo_forces_flown=True` on a PMP row now means Stage 1 applied them; Stage 2 is inertial.
- The propellant of a PMP decision vector does not change with the Stage-1 physics (burn durations
  are fixed by x) — check the terminal state, not the propellant, when re-flying old x.
- `--budget P,G` also sets `PSO_N_PARTICLES`/`PSO_MAX_GENERATIONS` (PMP and reference fallback);
  pass those with `--set`. `--only show_` also selects the two segmented cases, which rebuild the
  tracked reference.
- A PMP swarm below ~250×1000 does not reach orbit. Plain Gauss–Newton stalls on the condition
  system; use `least_squares(method="lm")` + γ_p continuation.
- `ecef_to_eci_velocity` keeps γ — only valid near γ = 0. `pso_tail_improvement_frac ≈ 0` means the
  swarm stalled, not that it is optimal.
- `dev-notes/handoff.md` and `handoff-2026-09-10.md` are stale; trust `Tese/worktree.md` for
  configuration.
